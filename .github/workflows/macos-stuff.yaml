name: macos stuff
on:
  workflow_dispatch:
  pull_request:

jobs:
  context:
    runs-on: macos-10.15
    steps:
    - uses: actions/checkout@v2
    - name: Install stuff
      run: |
        bash ./setup_deps.sh --set-gmake-as-make-on-macos
